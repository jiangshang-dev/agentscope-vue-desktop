核心就一句话：**贴进激活页的是「激活码」；打包时写进软件的是「签发口令」。两边口令必须一致，激活码还不能改坏。**

### 两个东西别混

| 名称 | 是什么 | 干什么 |
|------|--------|--------|
| `LICENSE_HMAC_SECRET` / 脚本里的 `SECRET` | 签发口令（公章） | 打包时写进客户端 + 生成激活码时用 |
| `AGOS.xxx.yyy` | 激活码 | 只给用户粘贴到「软件激活」页 |

文档里的 `LICENSE_HMAC_SECRET='生产密钥'` 只是占位，**不要真填「生产密钥」三个字**。

### 你现在为什么失败

1. 截图里那串 payload 已经对不上正常的 `{"v":1,"plan":"pro",...}`，多半是复制时改坏了 → 会报「签名校验失败」
2. 若打包时设过自定义口令，却用默认 DEV 生成的码，也会失败
3. 设备指纹是激活**之后**绑定用的，**生成密钥时不用填指纹**

### 打包时没设过自定义口令（最常见）

客户端默认口令是：

`agentscope-desktop-dev-secret-change-me`

在桌面端工程重新生成，**整行复制**：

```bash
cd agentscope-vue-desktop
pnpm run gen:license -- --forever --plan pro --uid customer-a
```

或用 API 仓库脚本（默认 SECRET 相同）：

```bash
cd agentscope-api
python3 激活生成序列号.py --forever --plan pro --uid customer-a
```

正确激活码应类似（以你终端新生成的为准）：

`AGOS.eyJ2IjoxLCJwbGFuIjoicHJvIiwidWlkIjoiY3VzdG9tZXItYSIsImV4cCI6MH0....`

中间必须能看出是 `plan":"pro"` 那段，不能缺字。

### 若打包时用过自定义口令

签发和打包必须同一串，例如：

```bash
# 先想好一串，两边共用（示例）
export LICENSE_HMAC_SECRET='你自己的长随机串'

# 1) 用同一串重新打包桌面端
LICENSE_HMAC_SECRET="$LICENSE_HMAC_SECRET" pnpm run dist:mac
LICENSE_HMAC_SECRET="abc" pnpm run dist:mac

# 2) 用同一串生成激活码
LICENSE_HMAC_SECRET="$LICENSE_HMAC_SECRET" pnpm run gen:license -- --forever --plan pro --uid customer-a
```

Python 脚本则把文件顶部的 `SECRET` 改成同一串，或：

```bash
python3 激活生成序列号.py --secret '你自己的长随机串' --forever --plan pro --uid customer-a
```

### 小结

- 激活页粘贴：**AGOS. 开头的整行激活码**
- 不是粘贴 `LICENSE_HMAC_SECRET`
- 口令一致 + 完整复制，就能过；你现在这串多半是码坏了或口令对不上